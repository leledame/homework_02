<div align="center">

# 🏠 **HOMEWORK 02**
</div>

Я сделал 007 и 008, надеюсь правильно. 009 допишу чуть позже, руки ещё не дошли 😅.

---

### 📦 Что по коду?

**007**: Задаём два числа `a` и `b` прямо в коде, выводим на экран математические операции вида `a + b = ...` в сторогом
порядке. Использую `float` потому что идея жалуется на int (always is 0).

**008**: Задаём цифру `k`, выводим таблицу умножения от 2 до 9 через цикл `for`.

---

### 🚀 Команды запуска

```powershell
javac src/Main.java
java -cp src Main
```